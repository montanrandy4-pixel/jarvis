{
  "name": "shopkeeper-client",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard: plain HTML and Tailwind, served by the backend.",
  "comment": "Deliberately has no build step and no dependencies. Tailwind comes from its CDN and the page is static, so there is nothing to compile, bundle or keep up to date. The backend serves this directory."
}
